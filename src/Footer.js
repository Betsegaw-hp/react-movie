import React from 'react';
import bouncer from './chevron-up.svg';

function Footer() {
  return (
      <footer className="footer">
      <div>footer</div>
      <div className="top-link">
        <i src={bouncer}></i>
      </div>
  </footer>
  );
}

export default Footer;

// arrow bouncer
// git icon
// link the characters to more info
// pagination ( ideal)
// adding episodes ( ideal)
